{
  "moduleInfo": {
    "moduleName": "AnikotoTV",
    "moduleInitials": "AK",
    "moduleDesc": "Public-page AnikotoTV browsing with visible SUB/HSUB/DUB server selection and direct in-app WebView playback.",
    "changelog": "v1.0: Home and search browsing, title metadata, dynamically loaded episode lists, public server choices, direct embed WebView playback, and a normal episode-page fallback.",
    "developer": "yungfiga",
    "moduleID": "anikototv-to-public-server-webview-v100",
    "moduleImage": "https://anikototv.to/AnikotoTheme/assets/images/favicon.png",
    "moduleVersion": 1.0,
    "moduleLenguage": "ENG",
    "moduleType": "Video",
    "baseURL": "https://anikototv.to/",
    "moduleDeveloperSite": "",
    "UpdateSite": "",
    "preferedServer": "",
    "preferedDownloadServer": "",
    "blackListed": [
      ""
    ]
  },
  "global": {
    "variables": [
      {
        "key": "",
        "value": ""
      }
    ],
    "cookies": [
      {
        "key": "",
        "value": ""
      }
    ],
    "headers": [
      {
        "key": "Referer",
        "value": "https://anikototv.to/"
      },
      {
        "key": "Accept-Language",
        "value": "en-US,en;q=0.9"
      }
    ]
  },
  "mainPage": [
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/home",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    }
  ],
  "search": [
    {
      "request": {
        "url": "https://anikototv.to/filter?keyword=<searched>",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "separator": "%20",
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/filter?keyword=<searched>",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "separator": "%20",
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    },
    {
      "request": {
        "url": "https://anikototv.to/filter?keyword=<searched>",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "separator": "%20",
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Listing ( request, extra, javascriptConfig, output ) {\n        this.request = request;\n        this.extra = extra;\n        this.separator = '';\n        this.javascriptConfig = javascriptConfig;\n        this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Insets ( top, bottom, left, right ) {\n        this.top = top; this.bottom = bottom; this.left = left; this.right = right;\n    }\n    function Size ( width, height ) { this.width = width; this.height = height; }\n    function Ratio ( inRelation, number1, number2 ) {\n        this.inRelation = inRelation; this.number1 = number1; this.number2 = number2;\n    }\n    function Section ( sectionName, separator ) { this.sectionName = sectionName; this.separator = separator; }\n    function Layout ( insets, visibleCellsWidthS, visibleCellsWidthM, visibleCellsWidthL,\n        visibleCellsHeight, heightForVisibleCells, cellSize, ratio, constant,\n        horizontalSpacing, verticalSpacing ) {\n        this.insets = insets;\n        this.visibleCellsWidthS = visibleCellsWidthS;\n        this.visibleCellsWidthM = visibleCellsWidthM;\n        this.visibleCellsWidthL = visibleCellsWidthL;\n        this.visibleCellsHeight = visibleCellsHeight;\n        this.heightForVisibleCells = heightForVisibleCells;\n        this.cellSize = cellSize;\n        this.ratio = ratio;\n        this.constant = constant;\n        this.horizontalSpacing = horizontalSpacing;\n        this.verticalSpacing = verticalSpacing;\n    }\n    function Output ( cellDesing, orientation, defaultLayout, paging, section, layout, data ) {\n        this.cellDesing = cellDesing; this.orientation = orientation;\n        this.defaultLayout = defaultLayout; this.paging = paging;\n        this.section = section; this.layout = layout; this.data = data;\n    }\n    function Data ( image, title, description, field1, field2, field3, field4,\n        isChapter, link, openInWebView ) {\n        this.image = image; this.title = title; this.description = description;\n        this.field1 = field1; this.field2 = field2; this.field3 = field3; this.field4 = field4;\n        this.isChapter = isChapter; this.link = link; this.openInWebView = openInWebView;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function titleUrl ( value ) {\n        var url = absolute( value );\n        return url.replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' );\n    }\n    function layout ( ) {\n        return new Layout(\n            new Insets( 10, 10, 10, 10 ), 2, 3, 4, 1, 360,\n            new Size( 240, 360 ), new Ratio( 'width', 2, 3 ),\n            new Size( 0, 0 ), 10, 12\n        );\n    }\n\n    var rows = [ ];\n    var seen = { };\n    Array.from( document.querySelectorAll( '.item' ) ).forEach( function ( item ) {\n        var anchor = item.querySelector( '.info a.name[href*=\"/watch/\"]' ) ||\n            item.querySelector( 'a[href*=\"/watch/\"]' );\n        var image = item.querySelector( 'img' );\n        var link = anchor ? titleUrl( anchor.getAttribute( 'href' ) ) : '';\n        var title = clean( anchor && anchor.textContent ) || clean( image && image.getAttribute( 'alt' ) );\n        if ( !link || !title || seen[ link ] ) return;\n        seen[ link ] = true;\n\n        var type = clean( item.querySelector( '.poster .meta .right' ) &&\n            item.querySelector( '.poster .meta .right' ).textContent );\n        var sub = clean( item.querySelector( '.ep-status.sub span' ) &&\n            item.querySelector( '.ep-status.sub span' ).textContent );\n        var dub = clean( item.querySelector( '.ep-status.dub span' ) &&\n            item.querySelector( '.ep-status.dub span' ).textContent );\n        var total = clean( item.querySelector( '.ep-status.total span' ) &&\n            item.querySelector( '.ep-status.total span' ).textContent );\n        var details = [ ];\n        if ( sub ) details.push( 'SUB ' + sub );\n        if ( dub ) details.push( 'DUB ' + dub );\n        if ( total ) details.push( total + ' total' );\n\n        rows.push( new Data(\n            request( image ? absolute( image.getAttribute( 'data-src' ) || image.getAttribute( 'src' ) ) : '', location.href ),\n            title,\n            details.join( ' • ' ),\n            type,\n            sub ? 'SUB ' + sub : '',\n            dub ? 'DUB ' + dub : '',\n            total ? total + ' episodes' : '',\n            false,\n            request( link, location.href ),\n            false\n        ) );\n    } );\n\n    if ( !rows.length ) {\n        rows.push( new Data(\n            request( '', location.href ),\n            'No titles found',\n            'The page did not expose any public title cards. Try the site fallback.',\n            '', '', '', '', false, request( location.href || ORIGIN + '/', ORIGIN + '/' ), true\n        ) );\n    }\n\n    var next = document.querySelector( 'a[rel=\"next\"], .pagination .next a, .pagination a[aria-label*=\"Next\"]' );\n    var nextUrl = next ? absolute( next.getAttribute( 'href' ) ) : '';\n    var output = [ new Output(\n        'wide8', 'vertical', '', '', new Section(\n            location.pathname.indexOf( '/filter' ) === 0 ? 'Search Results' : 'Latest Releases', true\n        ), layout( ), rows\n    ) ];\n    var savedData = document.getElementById( 'ketsu-final-data' );\n    var listing = new Listing(\n        request( nextUrl, location.href ),\n        new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n        new JavascriptConfig( false, false, '' ),\n        output\n    );\n    savedData.innerHTML = JSON.stringify( listing );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": [
        {
          "cellDesing": "normal1",
          "orientation": "vertical",
          "defaultLayout": "wide",
          "paging": "",
          "section": {
            "sectionName": "",
            "separator": false
          },
          "layout": {
            "insets": {
              "top": 0,
              "bottom": 0,
              "left": 0,
              "right": 0
            },
            "visibleCellsWidthS": 2,
            "visibleCellsWidthM": 2,
            "visibleCellsWidthL": 2,
            "visibleCellsHeight": 2,
            "heightForVisibleCells": 400,
            "cellSize": {
              "width": 300,
              "height": 300
            },
            "ratio": {
              "inRelation": "width",
              "number1": 1,
              "number2": 2
            },
            "constant": {
              "width": 1,
              "height": 2
            },
            "horizontalSpacing": 0,
            "verticalSpacing": 0
          },
          "data": [
            {
              "image": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              },
              "title": "",
              "description": "",
              "field1": "",
              "field2": "",
              "field3": "",
              "field4": "",
              "openInWebView": false,
              "isChapter": false,
              "link": {
                "url": "",
                "method": "get",
                "headers": [
                  {
                    "key": "",
                    "value": ""
                  }
                ],
                "httpBody": null
              }
            }
          ]
        }
      ]
    }
  ],
  "info": [
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Info ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Chapter ( chapName, link, openInWebView ) {\n        this.chapName = chapName; this.link = link; this.openInWebView = openInWebView;\n    }\n    function Output ( image, title, link, description, genres,\n        field1, field2, field3, field4, chapters ) {\n        this.image = image; this.title = title; this.link = link; this.description = description;\n        this.genres = genres; this.field1 = field1; this.field2 = field2;\n        this.field3 = field3; this.field4 = field4; this.chapters = chapters;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function absolute ( value ) {\n        try { return new URL( String( value || '' ), ORIGIN + '/' ).href; } catch ( ignored ) { return ''; }\n    }\n    function request ( url, referer ) {\n        return new ModuleRequest( String( url || '' ), 'get', [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ], null );\n    }\n    function baseTitleUrl ( value ) {\n        return absolute( value ).replace( /\\/ep-[^/?#]+(?:[/?#].*)?$/i, '' ).replace( /#.*$/, '' );\n    }\n    function meta ( label ) {\n        var target = String( label || '' ).toLowerCase();\n        var nodes = Array.from( document.querySelectorAll( '#watch-main .bmeta .meta > div' ) );\n        for ( var i = 0; i < nodes.length; i++ ) {\n            var text = clean( nodes[ i ].textContent );\n            if ( text.toLowerCase().indexOf( target + ':' ) === 0 ) {\n                return clean( text.slice( text.indexOf( ':' ) + 1 ) );\n            }\n        }\n        return '';\n    }\n    function emit ( ) {\n        var pageUrl = baseTitleUrl( location.href );\n        var titleNode = document.querySelector( '#watch-main h1[itemprop=\"name\"], #watch-main h1.title' );\n        var imageNode = document.querySelector( '#watch-main img[itemprop=\"image\"]' );\n        var title = clean( titleNode && titleNode.textContent ) || 'AnikotoTV title';\n        var synopsis = clean( document.querySelector( '#watch-main .synopsis .content' ) &&\n            document.querySelector( '#watch-main .synopsis .content' ).textContent );\n        var genres = Array.from( document.querySelectorAll( '#watch-main a[href*=\"/genre/\"]' ) )\n            .map( function ( a ) { return clean( a.textContent ); } ).filter( Boolean );\n        var seen = { };\n        genres = genres.filter( function ( genre ) {\n            var key = genre.toLowerCase(); if ( seen[ key ] ) return false; seen[ key ] = true; return true;\n        } );\n\n        var chapters = [ ];\n        Array.from( document.querySelectorAll( '#w-episodes a[href*=\"/watch/\"]' ) ).forEach( function ( a ) {\n            var href = absolute( a.getAttribute( 'href' ) );\n            var number = clean( a.getAttribute( 'data-num' ) || a.getAttribute( 'data-slug' ) || a.textContent );\n            if ( !href || seen[ 'episode:' + href ] ) return;\n            seen[ 'episode:' + href ] = true;\n            var badges = [ ];\n            if ( clean( a.getAttribute( 'data-sub' ) ) && a.getAttribute( 'data-sub' ) !== '0' ) badges.push( 'SUB' );\n            if ( clean( a.getAttribute( 'data-dub' ) ) && a.getAttribute( 'data-dub' ) !== '0' ) badges.push( 'DUB' );\n            chapters.push( new Chapter(\n                'Episode ' + ( number || clean( a.textContent ) || '?' ) +\n                    ( badges.length ? ' • ' + badges.join( '/' ) : '' ),\n                request( href, pageUrl ),\n                false\n            ) );\n        } );\n        chapters.sort( function ( a, b ) {\n            var an = Number( ( a.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            var bn = Number( ( b.chapName.match( /Episode\\s+([\\d.]+)/i ) || [ 0, 0 ] )[ 1 ] );\n            return an - bn;\n        } );\n        if ( !chapters.length ) {\n            chapters.push( new Chapter( 'Open title page in WebView', request( pageUrl, ORIGIN + '/' ), true ) );\n        }\n\n        var type = meta( 'Type' );\n        var status = meta( 'Status' );\n        var score = meta( 'MAL' );\n        var duration = meta( 'Duration' );\n        var episodeCount = meta( 'Episodes' );\n        var detail = new Output(\n            request( imageNode ? absolute( imageNode.getAttribute( 'src' ) || imageNode.getAttribute( 'data-src' ) ) : '', pageUrl ),\n            title,\n            request( pageUrl, ORIGIN + '/' ),\n            synopsis,\n            genres,\n            type,\n            status,\n            score ? 'MAL ' + score : duration,\n            episodeCount ? 'Episodes: ' + episodeCount : duration,\n            chapters\n        );\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var info = new Info(\n            request( '', pageUrl ),\n            new Extra( [ new Commands( '', EMPTY ) ], EMPTY ),\n            new JavascriptConfig( false, false, '' ),\n            detail\n        );\n        savedData.innerHTML = JSON.stringify( info );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForEpisodes ( ) {\n        if ( document.querySelector( '#w-episodes a[href*=\"/watch/\"]' ) || attempts >= 20 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForEpisodes, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "image": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "link": {
          "url": "",
          "method": "get",
          "headers": [
            {
              "key": "",
              "value": ""
            }
          ],
          "httpBody": null
        },
        "title": "",
        "description": "",
        "genres": [
          ""
        ],
        "field1": "",
        "field2": "",
        "field3": "",
        "field4": "",
        "chapters": [
          {
            "chapName": "",
            "openInWebView": false,
            "link": {
              "url": "empty",
              "method": "get",
              "headers": [
                {
                  "key": "key",
                  "value": "value"
                }
              ],
              "httpBody": null
            }
          }
        ]
      }
    }
  ],
  "chapters": [
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    },
    {
      "request": {
        "url": "",
        "method": "get",
        "headers": [
          {
            "key": "Referer",
            "value": "https://anikototv.to/"
          },
          {
            "key": "Accept-Language",
            "value": "en-US,en;q=0.9"
          }
        ],
        "httpBody": null
      },
      "extra": {
        "commands": [
          {
            "commandName": "",
            "params": [
              {
                "key": "",
                "value": ""
              }
            ]
          }
        ],
        "extraInfo": [
          {
            "key": "",
            "value": ""
          }
        ]
      },
      "javascriptConfig": {
        "removeJavascript": false,
        "loadInWebView": false,
        "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n\n    function Chapters ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( videos, images, text ) { this.videos = videos; this.images = images; this.text = text; }\n    function Videos ( needsResolver, rawVideo ) {\n        this.needsResolver = needsResolver; this.rawVideo = rawVideo;\n    }\n    function NeedsResolver ( resolverIdentifier, link ) {\n        this.resolverIdentifier = resolverIdentifier; this.link = link;\n    }\n\n    var ORIGIN = 'https://anikototv.to';\n    var EMPTY = [ new KeyValue( '', '' ) ];\n    function clean ( value ) { return String( value || '' ).replace( /\\s+/g, ' ' ).trim(); }\n    function request ( url, referer, ajax ) {\n        var headers = [\n            new KeyValue( 'Referer', referer || ORIGIN + '/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ];\n        if ( ajax ) {\n            headers.push( new KeyValue( 'X-Requested-With', 'XMLHttpRequest' ) );\n            headers.push( new KeyValue( 'Accept', 'application/json, text/plain, */*' ) );\n        }\n        return new ModuleRequest( String( url || '' ), 'get', headers, null );\n    }\n    function serverName ( value ) {\n        var text = clean( value ).toLowerCase();\n        if ( text.indexOf( 'vidstream' ) !== -1 ) return 'Vidstream-2';\n        if ( text.indexOf( 'vidplay' ) !== -1 ) return 'VidPlay-1';\n        if ( text.indexOf( 'hd' ) !== -1 ) return 'HD-1';\n        return 'Other';\n    }\n    function audioType ( value ) {\n        var type = clean( value ).toLowerCase();\n        if ( type === 'sub' ) return 'SUB';\n        if ( type === 'hsub' ) return 'HSUB';\n        if ( type === 'dub' ) return 'DUB';\n        return 'OTHER';\n    }\n    function emit ( ) {\n        var pageUrl = location.href.replace( /#.*$/, '' );\n        var choices = [ ];\n        var seen = { };\n        Array.from( document.querySelectorAll( '#w-servers .type[data-type]' ) ).forEach( function ( group ) {\n            var type = audioType( group.getAttribute( 'data-type' ) );\n            Array.from( group.querySelectorAll( 'li[data-link-id]' ) ).forEach( function ( item ) {\n                var linkId = clean( item.getAttribute( 'data-link-id' ) );\n                if ( !linkId || seen[ linkId ] ) return;\n                seen[ linkId ] = true;\n                var server = serverName( item.textContent );\n                choices.push( new NeedsResolver(\n                    'AnikotoTV Embed - ' + type + ' - ' + server,\n                    request( ORIGIN + '/ajax/server?get=' + encodeURIComponent( linkId ), pageUrl, true )\n                ) );\n            } );\n        } );\n        choices.push( new NeedsResolver(\n            'AnikotoTV Web Player - Normal episode page',\n            request( pageUrl, ORIGIN + '/', false )\n        ) );\n\n        var savedData = document.getElementById( 'ketsu-final-data' );\n        var chapter = new Chapters(\n            request( '', pageUrl, false ),\n            new Extra( [ new Commands( '', EMPTY ) ], [\n                new KeyValue( 'publicServerCount', String( Math.max( 0, choices.length - 1 ) ) ),\n                new KeyValue( 'fallback', pageUrl )\n            ] ),\n            new JavascriptConfig( false, false, '' ),\n            new Output(\n                new Videos( choices, [ ] ),\n                [ ],\n                { text: choices.length > 1\n                    ? 'Choose a public SUB, HSUB, or DUB server. The normal episode page is always the final fallback.'\n                    : 'No public server list appeared in time. Use the normal episode page fallback.' }\n            )\n        );\n        savedData.innerHTML = JSON.stringify( chapter );\n        window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n    }\n\n    var attempts = 0;\n    ( function waitForServers ( ) {\n        if ( document.querySelector( '#w-servers li[data-link-id]' ) || attempts >= 24 ) {\n            emit( ); return;\n        }\n        attempts += 1; setTimeout( waitForServers, 250 );\n    } )( );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
      },
      "output": {
        "videos": {
          "needsResolver": [],
          "rawVideo": []
        },
        "images": [],
        "text": null
      }
    }
  ],
  "moduleResolvers": [
    {
      "resolverInfo": {
        "baseURL": "https://anikototv.to/ajax/server",
        "developer": "AnikotoTV public server-response handoff",
        "moduleID": "anikototv-public-embed-resolver-v100",
        "nameMatches": [
          "AnikotoTV Embed - SUB - HD-1",
          "AnikotoTV Embed - SUB - Vidstream-2",
          "AnikotoTV Embed - SUB - VidPlay-1",
          "AnikotoTV Embed - SUB - Other",
          "AnikotoTV Embed - HSUB - HD-1",
          "AnikotoTV Embed - HSUB - Vidstream-2",
          "AnikotoTV Embed - HSUB - VidPlay-1",
          "AnikotoTV Embed - HSUB - Other",
          "AnikotoTV Embed - DUB - HD-1",
          "AnikotoTV Embed - DUB - Vidstream-2",
          "AnikotoTV Embed - DUB - VidPlay-1",
          "AnikotoTV Embed - DUB - Other",
          "AnikotoTV Embed - OTHER - HD-1",
          "AnikotoTV Embed - OTHER - Vidstream-2",
          "AnikotoTV Embed - OTHER - VidPlay-1",
          "AnikotoTV Embed - OTHER - Other"
        ],
        "resolverName": "AnikotoTV Public Embed",
        "resolverVersion": 1.0
      },
      "resolver": [
        {
          "request": {
            "url": "",
            "method": "get",
            "headers": [
              {
                "key": "",
                "value": ""
              }
            ],
            "httpBody": null
          },
          "extra": {
            "commands": [
              {
                "commandName": "",
                "params": [
                  {
                    "key": "",
                    "value": ""
                  }
                ]
              }
            ],
            "extraInfo": [
              {
                "key": "",
                "value": ""
              }
            ]
          },
          "javascriptConfig": {
            "removeJavascript": true,
            "loadInWebView": false,
            "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n    function Resolver ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( video ) { this.video = video; }\n\n    function responseJson ( ) {\n        var values = [ ];\n        var pre = document.querySelector( 'pre' );\n        if ( pre ) values.push( pre.textContent );\n        if ( document.body ) {\n            values.push( document.body.innerText );\n            values.push( document.body.textContent );\n        }\n        for ( var i = 0; i < values.length; i++ ) {\n            try { return JSON.parse( String( values[ i ] || '' ).trim( ) ); } catch ( ignored ) {}\n        }\n        return null;\n    }\n\n    var payload = responseJson( );\n    var embed = payload && Number( payload.status ) === 200 &&\n        payload.result && typeof payload.result.url === 'string'\n        ? payload.result.url : '';\n    var empty = [ new KeyValue( '', '' ) ];\n    var resolver = new Resolver(\n        new ModuleRequest( embed, 'get', embed ? [\n            new KeyValue( 'Referer', 'https://anikototv.to/' ),\n            new KeyValue( 'Accept-Language', 'en-US,en;q=0.9' )\n        ] : empty, null ),\n        new Extra( [ new Commands( '', empty ) ], empty ),\n        new JavascriptConfig( true, Boolean( embed ), '' ),\n        new Output( [ ] )\n    );\n    document.getElementById( 'ketsu-final-data' ).innerHTML = JSON.stringify( resolver );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    console.error( error && error.message ? error.message : error );\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
          },
          "output": {
            "moduleID": "",
            "video": []
          }
        }
      ]
    },
    {
      "resolverInfo": {
        "baseURL": "https://anikototv.to/watch/",
        "developer": "AnikotoTV normal episode-page fallback",
        "moduleID": "anikototv-normal-web-player-v100",
        "nameMatches": [
          "AnikotoTV Web Player - Normal episode page"
        ],
        "resolverName": "AnikotoTV Normal Web Player",
        "resolverVersion": 1.0
      },
      "resolver": [
        {
          "request": {
            "url": "",
            "method": "get",
            "headers": [
              {
                "key": "",
                "value": ""
              }
            ],
            "httpBody": null
          },
          "extra": {
            "commands": [
              {
                "commandName": "",
                "params": [
                  {
                    "key": "",
                    "value": ""
                  }
                ]
              }
            ],
            "extraInfo": [
              {
                "key": "",
                "value": ""
              }
            ]
          },
          "javascriptConfig": {
            "removeJavascript": true,
            "loadInWebView": true,
            "javaScript": "\ntry {\n    var KETSU_ASYNC = true;\n    function Resolver ( request, extra, javascriptConfig, output ) {\n        this.request = request; this.extra = extra;\n        this.javascriptConfig = javascriptConfig; this.output = output;\n    }\n    function ModuleRequest ( url, method, headers, httpBody ) {\n        this.url = url; this.method = method; this.headers = headers; this.httpBody = httpBody;\n    }\n    function Extra ( commands, extraInfo ) { this.commands = commands; this.extraInfo = extraInfo; }\n    function Commands ( commandName, params ) { this.commandName = commandName; this.params = params; }\n    function JavascriptConfig ( removeJavascript, loadInWebView, javaScript ) {\n        this.removeJavascript = removeJavascript; this.loadInWebView = loadInWebView; this.javaScript = javaScript;\n    }\n    function KeyValue ( key, value ) { this.key = key; this.value = value; }\n    function Output ( video ) { this.video = video; }\n    var empty = [ new KeyValue( '', '' ) ];\n    var resolver = new Resolver(\n        new ModuleRequest( '', 'get', empty, null ),\n        new Extra( [ new Commands( '', empty ) ], empty ),\n        new JavascriptConfig( false, true, '' ),\n        new Output( [ ] )\n    );\n    document.getElementById( 'ketsu-final-data' ).innerHTML = JSON.stringify( resolver );\n    window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' );\n} catch ( error ) {\n    try { window.webkit.messageHandlers.EXECUTE_KETSU_ASYNC.postMessage( '' ); } catch ( ignored ) {}\n}\n"
          },
          "output": {
            "moduleID": "",
            "video": []
          }
        }
      ]
    }
  ],
  "responseCodeFunctions": [],
  "helperFunctions": [],
  "moduleCreatorGlobalOptions": {
    "selected": "",
    "notesData": {
      "openedNotes": [],
      "activeNote": "",
      "notes": {}
    },
    "hideViews": [],
    "id": "anikototvPublicServerV100",
    "projectName": "AnikotoTV — Public Server WebView"
  }
}
